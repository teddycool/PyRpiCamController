# PyRpiCamController consolidated dependencies
# Install with:
#   pip install -r requirements.txt

# Runtime and tooling dependencies
numpy>=1.24
opencv-python>=4.8
requests>=2.28
flask>=2.2
simplejpeg>=1.7
pyserial>=3.5
paho-mqtt>=1.6

# Legacy/compatibility modules referenced in codebase
picamera>=1.13; platform_machine == 'armv7l' or platform_machine == 'aarch64'

# Test dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
mock>=4.0.0
jsonschema>=4.0.0
coverage>=7.0.0
