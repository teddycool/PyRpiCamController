# PyRpiCamController Python dependencies (runtime + tooling scripts)
# Installed by tools/install-all-optimized.py using pip.
# Hardware/system-specific libraries are installed via apt in the installer:
# - python3-picamera2
# - python3-libcamera
# - python3-rpi.gpio
# - python3-opencv, opencv-data (required; vision pipeline uses cv2)
# - python3-rpi-ws281x (with pip fallback)

# Core runtime
numpy>=1.24
requests>=2.28
flask>=2.2
simplejpeg>=1.7
pyserial>=3.5
paho-mqtt>=1.6

# Legacy/compatibility modules referenced in codebase
picamera>=1.13
